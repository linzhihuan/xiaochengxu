<view class="detail-page">
  <view class="back-nav" style="top:{{statusBarHeight}}px" bindtap="goBack">← BACK</view>

  <scroll-view scroll-y class="scroll-container">
    <image class="hero-image" src="{{info.img}}" mode="aspectFill"></image>
    
    <view class="detail-content">
      <view class="meta">
        <text class="tag">{{info.tag}}</text>
        <text class="title">{{info.title}}</text>
      </view>
      <view class="desc">
        捕捉光影的细微之处，记录真实的情感流动。每一个快门都是对当下的致敬。
      </view>
      
      <view class="gallery">
        <image wx:for="{{gallery}}" wx:key="*this" src="{{item}}" mode="widthFix" class="g-img"></image>
      </view>
    </view>
    
    <view style="height: 240rpx;"></view>
  </scroll-view>

  <view class="capsule-bar fade-in-up">
    <view class="left-actions">
      <button class="c-btn" open-type="contact">
        <text class="c-icon">💬</text>
        <text class="c-txt">Ask</text>
      </button>
      <button class="c-btn" open-type="share">
        <text class="c-icon">📤</text>
        <text class="c-txt">Share</text>
      </button>
      <view class="c-btn" bindtap="toggleFavorite">
        <text class="c-icon {{isFavorite ? 'active' : ''}}">{{isFavorite ? '♥' : '♡'}}</text>
        <text style="margin-top: -10rpx;" class="c-txt">Save</text>
      </view>
    </view>
    <view class="right-action" bindtap="goBooking">
      BOOK NOW
    </view>
  </view>
</view>